<UserControl x:Class="ChartCenter.WPFUserControl.BoomCatalogContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChartCenter.WPFUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="auto" Height="auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">


    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="BoomCatalogTemplate"  >
                <local:BoomCatalog></local:BoomCatalog>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>


    <Grid>
 
   <!--     <Expander>
            <ListBox Name="boomCatalogContainer"  >

                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}"  >
                        <EventSetter Event="MouseDoubleClick" Handler="Control_OnMouseDoubleClick"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Expander>-->
        <ListBox ItemTemplate="{StaticResource BoomCatalogTemplate}">
            
        </ListBox>
    </Grid>
</UserControl>
